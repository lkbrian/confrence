export type Speaker = {
  name: string
  ministry: string
  bio: string
  image: string
}

export type ScheduleDay = {
  day: string
  date: string
  items: string[]
}

export type Testimonial = {
  quote: string
  name: string
}
